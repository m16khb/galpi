# Qwen3 candidate transcription stack. Isolated from the pinned WhisperX
# environment so the two presets never share dependency versions.
# qwen-asr is young: 0.0.6 is the newest PyPI release and its API is still
# moving, so it stays pinned exactly until a verified upgrade lands.
qwen-asr==0.0.6
torch==2.8.0
torchaudio==2.8.0
pyannote.audio==4.0.7
imageio-ffmpeg==0.6.0
