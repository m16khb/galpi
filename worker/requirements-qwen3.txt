# Direct pins. `requirements-qwen3.lock` is the resolved set the app installs from;
# regenerate it with `uv pip compile` after editing anything here.
# Qwen3 candidate transcription stack (MLX runtime). Isolated from the pinned
# WhisperX environment so the two presets never share dependency versions.
# mlx-qwen3-asr runs the ASR model and the forced aligner natively on the
# Metal GPU with 8-bit weights converted during prepare; torch remains for
# the pyannote diarization stage only.
mlx-qwen3-asr[aligner]==0.3.5
mlx==0.32.1
torch==2.8.0
# pyannote.audio imports torchaudio at module scope; Galpi never calls it
# (wave-based IO), but the import must resolve for diarization to load.
torchaudio==2.8.0
pyannote.audio==4.0.7
imageio-ffmpeg==0.6.0
